import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:emergency_group/app/modules/group/group_module.dart';

void main() {
  initModule(GroupModule());
  // GroupController group;
  //
  setUp(() {
    //     group = GroupModule.to.get<GroupController>();
  });

  group('GroupController Test', () {
    //   test("First Test", () {
    //     expect(group, isInstanceOf<GroupController>());
    //   });

    //   test("Set Value", () {
    //     expect(group.value, equals(0));
    //     group.increment();
    //     expect(group.value, equals(1));
    //   });
  });
}
