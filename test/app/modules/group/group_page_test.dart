import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:emergency_group/app/modules/group/group_page.dart';

void main() {
  testWidgets('GroupPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(GroupPage(title: 'Group')));
    //  final titleFinder = find.text('Group');
    //  expect(titleFinder, findsOneWidget);
  });
}
